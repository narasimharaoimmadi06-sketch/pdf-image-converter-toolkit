{
  "frontend/src/tools/ImageToWord.tsx": "Tool component that converts an uploaded image (JPG/PNG/WebP) into a downloadable DOCX file by building a ZIP-based DOCX structure from scratch with embedded image XML. Fixed the Blob constructor type error by extracting a plain ArrayBuffer from the Uint8Array before passing it to Blob.",
  "frontend/src/tools/ImageCompressor.tsx": "Tool component that compresses an uploaded image using the Canvas API at a user-selected quality level (10–100%), displays original vs compressed file sizes, and provides a download button for the result.",
  "frontend/src/tools/ImageResizer.tsx": "Tool component that resizes an uploaded image to custom pixel dimensions using the Canvas API, with an aspect ratio lock toggle, live dimension preview, and download of the resized result.",
  "frontend/src/tools/MergePdfs.tsx": "Tool component accepting `onClose` prop that merges multiple PDFs using window.PDFLib with a null-guard check, file reordering, and a download button for the merged result.",
  "frontend/index.html": "HTML entry point that loads CDN scripts (PDF.js with workerSrc, jsPDF, pdf-lib, Mammoth.js) before the React module bundle, with a dark-themed inline loading indicator inside the root div so users see a styled placeholder instead of a blank/raw page while the bundle loads.",
  "frontend/src/index.css": "Global stylesheet with properly formatted OKLCH color tokens (using the `oklch()` wrapper) for both light and dark modes, Google Fonts imports, and utility classes for glassmorphism and gradient text effects.",
  "frontend/tailwind.config.js": "Tailwind configuration mapping CSS variable color tokens, Space Grotesk/Inter font families, custom border radii, box shadows, and animation keyframes.",
  "frontend/src/components/ErrorBoundary.tsx": "React class-based error boundary that catches unhandled component tree errors, supports an optional custom fallback prop, and displays a styled dark-themed fallback UI with \"Try Again\" and \"Reload Page\" buttons when no custom fallback is provided.",
  "frontend/src/App.tsx": "Main application component that wraps each major section (FloatingParticles, Hero, ToolGrid, Footer, ConversionModal) in its own ErrorBoundary so a failure in one section cannot crash the entire app, while managing active tool state and rendering the Toaster.",
  "frontend/src/tools/PdfToImage.tsx": "Tool component accepting `onClose` prop that converts PDF pages to PNG images using the window.pdfjsLib CDN global with a null-guard check, drag-and-drop upload, progress bar, and per-page download.",
  "frontend/src/tools/PdfToWord.tsx": "Tool component accepting `onClose` prop that extracts text from a PDF via window.pdfjsLib with a null-guard check and builds a DOCX ZIP file from scratch for download.",
  "frontend/src/tools/WordToPdf.tsx": "Tool component accepting `onClose` prop that converts a DOCX to PDF using window.mammoth and window.jspdf with null-guard checks for both CDN globals.",
  "frontend/src/tools/ImageToPdf.tsx": "Tool component accepting `onClose` prop that converts one or more images to a multi-page PDF using window.jspdf with a null-guard check, image previews, and remove-per-thumbnail support.",
  "frontend/src/components/FloatingParticles.tsx": "Decorative canvas animation component with comprehensive try/catch error handling around all canvas operations so any canvas failure silently stops the animation without crashing the rest of the app."
}